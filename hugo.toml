baseURL = "https://econ-blog.github.io/"
languageCode = "ko-kr"
defaultContentLanguage = "ko"
title = "쉽게 읽는 경제뉴스"
theme = "PaperMod"
enableRobotsTXT = true
pygmentsUseClasses = true

[services]
  [services.googleAnalytics]
    ID = "G-E2V0CFN172"

[params]
  defaultTheme = "auto"
  # PaperMod의 `_partials/author.html`과 `templates/schema_json.html`이 둘 다
  # `site.Params.author`를 읽는다 — 최상위 `author`(= Hugo의 `.Site.Author`)가
  # 아니다. 여기 두어야 <meta name="author">에 값이 차고 BlogPosting JSON-LD에
  # author 키가 생긴다. 감사 ⑤ D4가 그 셋을 각각 센다.
  author = "쉽게 읽는 경제뉴스"
  description = "어려운 경제·금융 뉴스를 비전공자도 이해할 수 있게 쉬운 말로 풀어 설명하고, 경제 용어를 사전으로 정리하는 블로그입니다."
  mainSections = ["posts"]
  ShowReadingTime = true
  ShowShareButtons = false
  ShowPostNavLinks = true
  ShowBreadCrumbs = true
  ShowCodeCopyButtons = false
  ShowToc = false

  [params.homeInfoParams]
    Title = "쉽게 읽는 경제뉴스"
    Content = "어려운 경제뉴스를 비전공자도 이해할 수 있게 풀어 설명하고, 경제 용어를 사전으로 정리합니다."

  [params.analytics]
    [params.analytics.google]
      SiteVerificationTag = "Pq-uzUwYArRYxLu2YzvnVhdM43JSCa7wQuHup-UJdGk"
    [params.analytics.naver]
      SiteVerificationTag = "9d0accb89d56184c70ab4aac11649e54d536dd34"

[menu]
  [[menu.main]]
    identifier = "posts"
    name = "뉴스 해설"
    url = "/posts/"
    weight = 1
  [[menu.main]]
    identifier = "dictionary"
    name = "경제 용어 사전"
    url = "/dictionary/"
    weight = 2

[taxonomies]
  tag = "tags"

[related]
  threshold = 10
  includeNewer = true
  toLower = false
  [[related.indices]]
    name = "tags"
    weight = 100
  [[related.indices]]
    name = "date"
    weight = 10

# llms.txt / llms-full.txt 를 정적 파일이 아니라 출력 형식으로 낸다.
# 손으로 적힌 `static/llms.txt` 는 61개 문서 중 15개만 담고 있었고, 매일 글이
# 나가는 저장소에서는 발행 첫날부터 어긋난다 — 렌더 경로로 옮겨 드리프트를 없앤다.
# 템플릿: `layouts/home.llms.txt` · `layouts/home.llmsfull.txt`.
[outputFormats]
  [outputFormats.llms]
    mediaType = "text/plain"
    baseName = "llms"
    isPlainText = true
    notAlternative = true
  [outputFormats.llmsfull]
    mediaType = "text/plain"
    baseName = "llms-full"
    isPlainText = true
    notAlternative = true

[outputs]
  home = ["HTML", "RSS", "JSON", "llms", "llmsfull"]
