# Hugo build output
/public/
/resources/_gen/
.hugo_build.lock

# macOS
.DS_Store

# Editors
.vscode/
.idea/

# Env / secrets
.env
.env.local

# Logs
*.log

# Non-blog artifacts (must not be published)
*.pdf
/tudul.md
# 설계 문서·SDD 산출물은 두지 않는다. 남길 것은 AGENTS.md / MEMORY.md에 직접 쓴다.
/docs/
.superpowers/

# 자격증명 — credentials.json 하나가 원본이며 GitHub Secret CREDENTIALS_JSON과 스키마가 같다.
credentials.json
creds.json
*.credentials.json
*-credentials.json
.venv/
# Python cache
__pycache__/
*.pyc
