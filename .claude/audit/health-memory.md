# 격주 점검 장기 기억

이 파일은 `/health-check`가 **자기 자신에게 2주 뒤에 보내는 편지**다.

점검 세션은 격주로, 매번 빈 맥락에서 시작한다. `report/health-*.md`는 그때의 관측을
남기지만 그것은 사람이 읽는 문서다 — 무엇을 왜 시도했고, 무엇을 기대했고, 그래서
다음번에 무엇을 봐야 하는지는 거기 없다. 그게 여기 있다.

## 읽는 법 (매 회차 §1에서 가장 먼저)

```bash
.venv/bin/python scripts/health_memory.py tail --entries 3
.venv/bin/python scripts/health_memory.py stats
```

통째로 Read 하지 않는다. 회차가 쌓이면 잘려서 읽히고, **잘렸다는 사실은 티가 나지
않는다** — 그냥 "지난번에 별일 없었나 보다"가 된다.

## 쓰는 법 (매 회차 마지막)

```bash
cat <<'EOF' | .venv/bin/python scripts/health_memory.py append
## YYYY-MM-DD · 회차 N
...
EOF
```

같은 날짜로 두 번 쓰면 덮어쓴다(재실행은 회차를 늘리지 않는다). 첫 줄 형식이 틀리면
거부한다 — 형식이 무너지면 읽기도 같이 무너진다.

## 회차 하나의 형식

```markdown
## YYYY-MM-DD · 회차 N

### 지난 회차에서 넘어온 것
- [닫힘] H-2026-09-10-1 · 무엇을 했고 결과가 무엇이었나 · 판정 근거
- [계속] H-2026-09-10-2 · 왜 아직 결론이 안 났나 · 언제 결론 나나
- [폐기] H-... · 왜 버렸나

### 지금 사이트의 상태
발행 N건 / 색인 N건 / GSC 28일 클릭 N · 노출 N / 평균 게재순위 N
지난 회차 대비: 클릭 ±N · 색인 ±N
(숫자만 적는다. 해석은 아래에)

### 무엇을 알게 됐나
- 관측과 해석을 분리해서 쓴다. "노출은 3배인데 클릭이 그대로다"(관측) /
  "제목이 검색 결과에서 안 눌리는 것으로 보인다"(해석).

### 이번에 시도한 것
- H-YYYY-MM-DD-1 · <무엇을 바꿨나> · <왜 그게 효과가 있다고 봤나>
  · 기대: <무엇이 어떻게 달라지면 성공인가 — 숫자로>
  · 확인: <몇 회차 뒤에 무엇을 보고 판정하나>
  · 범위: <건드린 파일 수와 종류>

### 하지 않기로 한 것
- <무엇을> · <왜 지금은 아닌가> — 다음번 내가 같은 것을 또 검토하지 않도록.

### 다음 회차의 나에게
- 가장 먼저 볼 것 한 가지
- 조심할 것 (건드렸다가 되돌린 것, 부작용이 늦게 나올 만한 것)
- 사람에게 물어봐야 할지 고민 중인 것
```

## 가설 ID 규칙

`H-<시작한 회차 날짜>-<번호>`. 한 번 붙인 ID는 바꾸지 않는다 — 여러 회차에 걸쳐
같은 이름으로 추적되는 것이 이 원장의 목적이다. 판정은 `닫힘`(효과 확인) ·
`폐기`(효과 없음 또는 전제가 틀림) · `계속` 셋뿐이며, **판정하지 않고 조용히 사라지게
두지 않는다.** 어느 쪽으로든 결론이 나야 다음 시도가 그 위에 쌓인다.

## 압축

`stats`의 `compact_due`가 참이면(1200줄 초과) 그 회차에 압축한다. 최근 6회차는 그대로
두고, 그보다 오래된 회차는 **`## 굳은 기억` 절 하나로** 접는다 — 닫히거나 폐기된 가설의
결론, 반복해서 확인된 사실, 하지 않기로 한 것의 이유만 남기고 그때의 수치와 과정은
버린다. 오래된 회차의 원문이 필요하면 git 히스토리에 있다.

<!-- 첫 회차는 아래에 붙는다. 이 주석 위는 머리말이며 회차가 아니다. -->

## 2026-08-27 · 회차 0

<!-- 무인 운영 전환을 한 세션이 남기는 인수인계. 점검이 실행한 회차가 아니라
     첫 회차가 빈손으로 시작하지 않도록 놓아두는 출발점이다. -->

### 지난 회차에서 넘어온 것
- 없음 (첫 기록)

### 지금 사이트의 상태
발행 34건 / 용어 28건 / 사이트 연령 40일
색인: 60건 중 2건 (홈·`/dictionary/`) — 나머지 대부분 "URL is unknown to Google"
네이버 색인 17건 (2026-08-23 사람이 직접 조회해 전달한 값)
GSC 28일: 이 시점 값은 `report/audit-2026-08-23.md` ② 참조

### 무엇을 알게 됐나
- **기술적 크롤 가능성은 결함이 없다.** 2026-08-23 SEO/GEO 크롤에서 깨진 내부 링크 0,
  홈에서 도달 불가 0, noindex 유출 0, canonical 위반 0. robots.txt는 전체 허용에
  AI 크롤러 8종 명시 허용.
- 그러므로 **색인이 안 되는 원인은 사이트 구조가 아니다.** 사이트 연령과 외부 신호
  부재이며, 저장소 안에서 고칠 수 있는 문제가 아니다. 이 결론을 회차마다 다시
  도출하지 마라 — 구조를 또 파는 것은 이미 두 번 했다.
- 2026-08-23 크롤에서 A5(llms.txt 25% 커버리지)·S3·S8·T1 일부는 그 자리에서 고쳤다.
  llms.txt는 정적 파일에서 Hugo 출력 형식(`layouts/home.llms.txt`)으로 옮겨 드리프트를
  구조적으로 없앴다. 이 코드가 다시 뜨면 그건 새 결함이 아니라 템플릿 회귀다.

### 이번에 시도한 것
- 시도 없음. 이 회차는 운영 구조 전환만 했다.

### 하지 않기로 한 것
- **색인 제출을 자동화하는 것** — GSC UI 수동 요청은 일일 한도가 있고 API 경로는
  사람의 브라우저 세션이 필요하다. `/audit-local`의 사람 대기열로 남긴다.
- **얇은 페이지(600자 미만) 분량 늘리기** — 산문 작성이라 점검의 일이 아니다.
  `/daily-post` 쪽 규약으로 풀어야 한다.

### 다음 회차의 나에게
- **가장 먼저 볼 것**: 무인 발행이 실제로 도는가. 2026-08-27에 승인 루프를 걷어내고
  `main` 직행으로 바꿨다. 그 뒤 첫 회차이므로 `content/posts/`의 `date`를 훑어
  **매일 한 건씩 나왔는지**, `draft: true`로 보류된 글이 쌓이지 않았는지 본다.
  보류가 3건 이상이면 발행 게이트가 상시로 걸린다는 뜻이고, 그건 게이트가 너무 빡빡한
  것인지 글이 실제로 나쁜 것인지 갈라야 한다.
- **조심할 것**:
  - `weekly-housekeeping.yml`은 2026-08-27 이전에 **한 번도 스케줄로 돈 적이 없다.**
    GitHub 내장 `schedule`을 그때 처음 걸었다. `report/housekeeping-*.md`가 실제로
    쌓이는지 확인하라 — 안 쌓이면 스케줄이 안 먹은 것이다.
  - `post-reviewer` 서브에이전트도 그때 새로 붙였다. 검토자가 매번 「보류」를 내서
    발행을 막고 있지는 않은지, 반대로 매번 「발행 가능」만 내서 형식적으로만 도는지
    양쪽을 다 의심하라. `/daily-post` 최종 보고에 판정과 차단 건수가 남는다.
- **사람에게 물어봐야 할지 고민 중인 것**: 없음. 광고·폐쇄 임계는 둘 다 한참 멀다
  (연령 40일 < 90일).

### 덧붙임 — 이관기의 이틀 (2026-08-28 기록)

`content/posts/`의 **2026-08-27과 2026-08-28 두 글은 사람 손으로 발행됐다.** 무인 경로가
낸 것이 아니므로 그 이틀을 무인 발행 성공 사례로 세지 마라.

- 08-27: 전환 PR을 만들던 중 승인 대기 중이던 PR #37이 고아가 되어 손으로 발행.
- 08-28: 전환 PR(#38)이 05:00 루틴보다 **늦게** 병합됐다. 루틴은 그 시점의 `main`에 있던
  옛 `daily-post.md`를 읽어 `auto/post-2026-08-28` + PR #39로 산출했고, 병합 뒤 집행할
  인박스가 없어 역시 손으로 발행했다(`0a31ebd`). 파이프라인은 정상이었고 버전만 옛것이었다.

여기서 얻은 것 하나: **루틴 세션은 명령 파일을 `main`에서 읽는다.** 명령 파일을 고치는
변경은 병합되기 전까지 아무 효력이 없고, 그 사이 루틴은 옛 규약대로 조용히 잘 돈다.
앞으로 파이프라인 자체를 바꿀 때는 다음 발화 시각 전에 병합됐는지를 반드시 확인하라.

**첫 무인 발행은 2026-08-29 05:00 KST 회차다.** 그것이 이 시스템의 진짜 첫 시험이고,
`post-reviewer` 게이트가 실제로 도는 첫 회차이기도 하다.
## 2026-08-28 · 회차 1

### 지난 회차에서 넘어온 것
- 회차 0이 남긴 것은 가설이 아니라 「먼저 볼 것」 셋이었다. 전부 확인했다.
  - **무인 발행이 도는가** -> 아직 판정 불가. 14일 연속 발행은 맞지만 08-27·08-28은
    사람 손이었고 무인 경로의 첫 시험은 **2026-08-29 05:00 KST**다. 다음 회차의 1차 확인 대상.
  - **유지보수가 쌓이는가** -> `report/housekeeping-*.md` 0건이지만 **고장이 아니다.**
    cron `0 18 * * 0`의 첫 발화는 2026-08-30 18:00 UTC로 아직 오지 않았다.
    회차 0이 경고한 함정에 그대로 걸릴 뻔했고, 실제로 걸리지 않았다.
  - **post-reviewer가 형식적으로 도는가** -> 판정 이력 0건이라 아직 볼 것이 없다.

### 지금 사이트의 상태
발행 39건 / 용어 34건 / 색인 60건 중 2건 / GSC 28일 클릭 0 · 노출 0 / 평균 게재순위 없음
GA4 28일: 활성 사용자 6 · 페이지뷰 14
사이트 연령 41일 · 사이트맵 93 URL (08-17 제출, 11일째 `lastDownloaded: null`)
지난 회차 대비: 발행 +5 · 색인 ±0

### 무엇을 알게 됐나
- (관측) 2026-08-14을 경계로 headings 계약 위반과 FAQ 누락이 **동시에** 0으로
  떨어졌고 15일째 유지 중이다. 같은 기간 볼드체 위반은 최근 11건 중 5건으로 계속됐다.
- (해석) 셋 다 `writing-styles.md`·`draft.md`에 강한 어조로 적힌 규칙이다. 차이는
  **결정론적 검사기의 유무 하나뿐**이다. 검사기가 붙은 규칙은 즉시 100% 수렴했고,
  안 붙은 규칙은 문구가 아무리 강해도 무작위로 어겨진다. LLM에게 규칙 문서를 강화하는
  것은 준수를 만들지 못한다 — 검사기가 만든다. 앞으로 산문 규칙을 새로 세울 때는
  "이것을 정규식으로 셀 수 있는가"를 먼저 묻는다.
- (관측) 렌더 산출물 기준 내부 링크 인바운드 0인 포스트는 **0건**, 중앙값 6건이다.
- (해석) 회차 0의 "구조가 원인이 아니다"는 **크롤 가능성** 축의 결론이었는데, 이번에
  **링크 위상**이라는 새 축에서도 깨끗했다. 두 축이 다 깨끗하므로 색인 정체의 원인은
  저장소 밖(도메인 연령·외부 신호)이라는 결론이 더 단단해졌다. **이 축을 또 파지 마라.**
- (함정 기록) 이 위상 측정을 처음에는 마크다운 원본으로 했고 "23건이 고립"이라는
  틀린 결론을 냈다. 원본에는 `related.html`이 만드는 링크가 없다. 두 번째로 렌더
  HTML을 봤지만 `--minify`가 속성 따옴표를 지워 정규식이 또 빗나갔다.
  **렌더 산출물을 볼 때는 minify를 전제하고 정규식을 짜라.**

### 이번에 시도한 것
- H-2026-08-28-1 · `quality.py`에 볼드 검사기 Q6 신설 + 14일 지난 위반 20건 정리
  · 왜: 위 「무엇을 알게 됐나」의 인과(검사기 있는 규칙만 수렴)를 그대로 적용한 것이다.
  · 기대: 2026-08-29 이후 발행분의 볼드 위반 0건 (기준선: 최근 11건 중 5건, 45%)
  · 확인: 다음 회차(2026-09-11)에 08-29~09-11 발행분을 Q6로 센다.
    0건이면 `닫힘`. 1건이라도 나오면 검사기만으로는 부족하다는 뜻이므로
    `daily-post.md` 게이트 배선이 병합됐는지부터 확인한다.
  · 범위: `.claude/audit/lib/quality.py` 1파일 + `content/posts/` 20파일
  · **절반만 작동한다**: Q6는 점검에게 보이게 할 뿐 발행을 막지 못한다. 차단은
    `daily-post.md` 게이트에 있고 그것은 지정 브랜치에 있다. 사람이 병합해야 완성된다.

### 하지 않기로 한 것
- **headings 계약 위반 22건 정리** — `content/` 20파일 상한을 볼드 정리로 채웠다.
  전부 08-13 이전 발행분이고 08-14 이후 재발 0건이므로 **고정 backlog이지 출혈이 아니다.**
  §7이 H2 제목 줄에는 14일 제한을 두지 않으므로 다음 회차에 20건을 한 번에 정리할 수 있다.
  **다음 회차 1순위.**
- **검색 페이지 신설** — `SearchAction`을 되살리는 방법이지만 색인이 병목인 상황에서
  색인 대상 URL만 늘린다. 거짓 선언을 지우는 쪽을 택했다. 색인이 붙은 뒤에 재검토.
- **`llms-full.txt` 롤링 윈도** — 실측 336KB(≈7~9만 토큰)로 아직 여유가 있다.
  하루 8.6KB씩 자라므로 **500KB에서 도입한다.** 지금 자르면 커버리지만 잃는다.
- **`attribution.py`의 -2 조정치 완화** — 래칫 가능성은 있으나 Corpus Gate가 막혀
  `topic-report.md` 자체가 없어 조정치가 적용되지 않는다. 신호 0인 상태에서 점수
  로직을 바꾸는 것은 근거 없는 변경이다. **게이트가 처음 열리는 회차에 재검토.**
- **주제·제목의 성과 기반 조정** — GSC 노출이 0행이라 CTR도 순위도 존재하지 않는다.
  측정 없이 조정하지 않는다.
- **N3 1건 수정** — 수치 판정이 걸려 있어 원문 대조 없이는 어느 쪽도 못 고친다. 대기열로.
- **AdSense·커스텀 도메인** — 임계 3개(클릭≥300·색인≥40·연령≥90일)를 전부 미달
  (0·2·41). 외부 리포트가 강하게 권했지만 임계는 트래픽 없이 수익화를 좇지 않으려고
  존재한다. 현재 값만 기록한다.

### 다음 회차의 나에게
- **가장 먼저 볼 것**: H-2026-08-28-1 판정. 08-29~09-11 발행분에 볼드가 있는가.
  있으면 검사기가 아니라 **게이트 배선(지정 브랜치)이 병합됐는지**를 먼저 의심하라 —
  회차 0이 배운 대로 루틴은 명령 파일을 `main`에서 읽는다.
- **그 다음**: headings backlog 22건 중 20건 정리. H2 제목 줄은 14일 제한이 없다.
- **조심할 것**:
  - 이번에 `layouts/partials/extend_head.html`에서 `potentialAction`을 제거하고
    `schema_json.html`의 `publisher.logo`를 바꿨다. Rich Results 쪽 부작용은 늦게
    나타난다 — 다음 회차에 홈·포스트 JSON-LD가 여전히 파싱되는지 다시 확인하라.
  - `corpus.py`를 `parents[2]`로 고쳤다. 이 값이 Corpus Gate와 광고·폐쇄 임계에
    들어가므로, 다음 회차에 `published_count`가 갑자기 커 보이면 그건 증가가 아니라
    **이번 수정으로 처음 제대로 세어진 것**이다. 추세로 오해하지 마라.
  - 외부에서 온 감사 리포트를 그대로 실행하지 마라. 이번 17건 중 3건은 근거가
    틀렸고(B2는 주석에 명시된 의도였다), 1건은 제안대로 하면 위반 0건짜리 무효과였다(C1).
    반대로 E1은 지적보다 원인이 더 깊었다. **전부 직접 재현한 뒤에 고쳤다.**
- **사람에게 물어봐야 할지 고민 중인 것**: 없음. 이번 회차의 대기열 6건은 전부
  사람만 할 수 있는 것(색인 제출·원문 대조)이거나 절대 금지 범위(워크플로)다.

### 덧붙임 — 같은 날 사람 지시로 바뀐 것 (2026-08-28 후속)

리포트를 커밋한 뒤 사람이 넷을 지시했다. **다음 회차가 위 본문만 읽고 판단하면 틀린다.**

- **H-2026-08-28-1은 이제 온전히 작동한다.** 지정 브랜치가 `main`에 병합돼
  `daily-post.md`의 볼드 게이트와 Hugo 빌드 확인이 발효됐다. 위 본문의 "절반만 작동한다"와
  "게이트 배선이 병합됐는지부터 의심하라"는 **더 이상 유효하지 않다.** 08-29 이후 발행분에
  볼드가 나오면 그것은 배선 문제가 아니라 게이트가 실제로 못 막은 것이다 — 그때는
  `quality.py`의 Q6 정규식이 그 형태를 놓쳤는지를 본다.
- **N3 오탐은 원문 대조 없이 닫혔다.** 검사기의 주어 귀속 버그였다. `indicator_of()`가
  scope 안 최장 용어를 고르는 탓에, 원인 지표와 결과 지표를 함께 말하는 문장에서 값이
  원인 쪽에 붙었다. `subject_conflicts()`로 값 앞 마지막 지표 명사구를 보게 했고 회귀
  테스트 2건을 붙였다. `numerics` total 1 -> 0. **이 계열의 오탐을 다시 보면 사람 대기열로
  올리기 전에 귀속 로직부터 의심하라** — 수치가 틀린 것처럼 보여도 검사기일 수 있다.
- **워크플로가 쓰기 가능해졌다.** `.github/workflows/**`가 절대 금지 목록에서 빠졌다.
  대신 **GitHub 내장 `schedule:`을 되살리는 것**이 새 금지다 — 발화가 신뢰할 수 없어
  cron-job.org로 이관했다. 새 주기 작업을 만들면 cron-job.org에 잡을 추가해야 하고,
  빠뜨리면 오류 없이 그냥 안 돈다.
- **유지보수 스케줄이 이관 중이다.** `weekly-housekeeping.yml`에서 내장 cron을 제거했고
  cron-job.org 잡(`0 2 * * 0` KST) 추가는 사람 대기열에 열려 있다. **다음 회차 §2에서
  `report/housekeeping-*.md`가 여전히 0건이면, 그것은 워크플로 고장이 아니라 그 잡이
  아직 안 만들어진 것이다.** 먼저 대기열을 확인하고 나서 고장을 의심하라.
